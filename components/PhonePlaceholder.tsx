import React from 'react'

type Props = {}

export default function PhonePlaceholder({}: Props) {
  return (
    <section className="bg-white lg:flex just items-center w-full h-full hidden">
    Phone Placeholder
  </section>
  )
}